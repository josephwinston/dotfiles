# /bin/csh -f

#
# $Id: //depot/Jody/main/jody/.powerpc-ibm-aix4.2.0.0/hls_ts/.cshrc#1 $
#

#
# Use the C Shell
#

set path = (/usr/sbin /sbin)
set path = ($path /sbin)
set path = ($path /bin /usr/bin /usr/bin/X11)
set path = ($path /usr/etc)
set path = ($path /usr/java/bin)
set path = ($path /usr/bsd)
set path = ($path /usr/gfx/)
set path = ($path /hls_reference/tools/bin)

# Set the TERM environment variable

# if ( -d /usr/lib/terminfo ) then
#    eval `tset -s -Q`
# endif

