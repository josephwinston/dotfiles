# -*- mode: snippet -*-
# name: class
# key: class
# binding: direct-keybinding
# expand-env: ((some-var some-value))
# type: command
# --
class ${1:Name}
{
public:
    ${1:$(yas-substr text "[^: ]*")}($2);
    virtual ~${1:$(yas-substr text "[^: ]*")}();
};				// class ${1:Name}